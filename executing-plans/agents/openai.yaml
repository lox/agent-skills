interface:
  display_name: "Executing Plans"
  short_description: "Execute plan slices locally"
  default_prompt: "Use $executing-plans to iterate on this plan locally in a branch, validate and auto-review each slice, commit completed slices, and finish with a walkthrough."
